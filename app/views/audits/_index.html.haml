- @audits.each do |audit|
  .panel.panel-default
    .panel-heading
      %h3.panel-title
        %b
          = audit.name
        | Start date:
        = audit.startDate
        | End date:
        = audit.endDate
        = link_to "Edit", edit_audit_path(audit), remote: true, class: "btn btn-default"
        = link_to "Delete", audit_delete_path(audit), remote: true, class: "btn btn-danger"
    .panel-body
      - audit.tasks.each do |task|
        %h5 #{task.descr} #{link_to "Delete", task_path(id: task.id), remote: true, method: :delete, class: "btn btn-danger"}
      = render "tasks/form", audit: audit
